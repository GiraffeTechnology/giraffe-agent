# market-reference-pricing — independent dependencies
# IMPORTANT: Do NOT merge these into the main giraffe-agent requirements.txt
# This module runs in an isolated container with its own dependency set.

# HTTP / scraping
requests==2.31.0
httpx==0.27.0
beautifulsoup4==4.12.3
lxml==5.1.0

# Database
sqlalchemy==2.0.28
alembic==1.13.1
psycopg2-binary==2.9.9

# API server
fastapi==0.110.0
uvicorn[standard]==0.29.0

# Data processing
numpy==1.26.4

# Qwen / DashScope structured extraction
dashscope==1.19.0

# Utilities
python-dotenv==1.0.1
pydantic==2.6.4
python-dateutil==2.9.0
pytz==2024.1

# Testing
pytest==8.1.0
pytest-asyncio==0.23.5
