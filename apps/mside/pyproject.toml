[project]
name = "giraffe-agent-mside"
version = "0.1.0-mvp"
description = "Giraffe Agent M-side — AI Merchandiser post-confirmation execution"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["../../"]
addopts = "-q"
